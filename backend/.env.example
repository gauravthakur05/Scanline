PORT=5000
FRONTEND_URL=http://localhost:5173

# Optional: without this key the app automatically runs in Demo Mode using
# the built-in deterministic analysis engine (no external calls made).
AI_API_KEY=
